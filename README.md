# Frontend

Este proyecto fue generado utilizando [Angular CLI](https://github.com/angular/angular-cli) versión 21.1.2.

## Servidor de desarrollo  

Para iniciar un servidor de desarrollo local, ejecuta:

```bash
ng serve
```

Una vez que el servidor esté en ejecución, abre tu navegador y navega a `http://localhost:4200/`. La aplicación se recargará automáticamente cada vez que modifiques alguno de los archivos fuente.

## Generación de código (Code scaffolding)  

Angular CLI incluye potentes herramientas de generación de código. Para crear un nuevo componente, ejecuta:

```bash
ng generate component component-name
```

Para ver la lista completa de esquemas disponibles (como `components`, `directives`, o `pipes`), ejecuta:

```bash
ng generate --help
```

## Compilación (Build)

Para compilar el proyecto, ejecuta:

```bash
ng build
```

Esto compilará tu proyecto y almacenará los artefactos de construcción en el directorio `dist/` Por defecto, la compilación en modo producción optimiza tu aplicación para rendimiento y velocidad.

## Ejecución de pruebas unitarias

Para ejecutar pruebas unitarias con el [Vitest](https://vitest.dev/) test runner, utiliza el siguiente comando:

```bash
ng test
```

## Pruebas end-to-end (e2e)  

Para realizar pruebas end-to-end, ejecuta:

```bash
ng e2e
```

Angular CLI no incluye por defecto un framework para pruebas end-to-end. Puedes elegir el que mejor se adapte a tus necesidades.

## Notas Adicionales
Recuerda: detrás de cada ng serve hay un desarrollador esperando que todo funcione a la primera 🤞

> Para más información sobre el uso de Angular CLI, incluyendo referencias detalladas de comandos, visita la página de Angular CLI Overview and Command Reference.

